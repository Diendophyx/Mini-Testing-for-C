#include <stdio.h>
#include <cs50.h>

int add(int a,int b);
int minus(int a,int b);

int main(void)
{
    // declaration of a function to get intitial two integrers
    int one = get_int("Number 1: ");
    int two = get_int("Number 2: ");

    switch(string - isRunning)
    {
        case subtract:
        minus(one,two);
        break;
        case add:
        add(one,two);
        break;
    }
}

int add(int a,int b)
{
    int x = a + b;
    printf("returned sum: %i\n", x);
    return 0;
}

int minus(int a,int b)
{
    int x = a - b;
    printf("Returned sum: %1\n", x)
    return 0;
}
